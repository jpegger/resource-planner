# Copy to secret.yaml, fill in values, apply with kubectl/oc. Do not commit real secrets.
apiVersion: v1
kind: Secret
metadata:
  name: resource-planner-db
type: Opaque
stringData:
  DATABASE_URL: "postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public"
